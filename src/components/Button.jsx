export const RoundButton = ({content}) => (
  <Button type={'round'} content={content} />
)
  
  export const EmojiButton = () => {
    return (
      <></>
      )
    }
    
const Button = ({type, children}) => (
  <button>{children}</button>
)
      